language: php
sudo: false
php:
  - 7.0
  - 7.1
  - hhvm
  - nightly

before_script:
  - composer install

script:
  - ./vendor/bin/phpcs --standard=vendor/internations/kodierungsregelwerksammlung/ruleset.xml ./src/ || true
  - phpunit

cache:
  directories:
    - vendor/
